namespace SR2MP.Client.Managers;

public class PlayerDataClientManager
{
    // this needs to be implemented -> new Packet that sends the data specified in
    // Server/Managers/PlayerDataManager.cs to the Server as a packet for the server to generate a persistent playerID,
    // which then also sends it back to Client and sets it as OwnPlayerID on Client end.

    // Do we even need this manager for this?
    // feel like we can handle that somewhere else, probably - we do not, this is resolved, file will be removed later
}